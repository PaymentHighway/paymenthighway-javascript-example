<p>
    <b>
        {{#if validRedirect }}
            Valid signature found
        {{else}}
            Signature was invalid
        {{/if}}
    </b>
</p>
<p> {{status}}</p>
<p>Response code: {{resultCode}}</p>
<p>Response message: {{message}}</p>
<p>Card token: {{cardToken}} <br />
{{#if cvcRequired }}
    <a href="/pay_with_token_and_cvc?token={{cardToken}}">Pay with token and cvc</a></p>
{{else}}
    <a href="/pay_with_token?token={{cardToken}}">Pay with token</a></p>
{{/if}}
<table>
    <tr>
        <th>name</th>
        <th>value</th>
    </tr>
    {{#each card}}
        <tr>
            <td>{{this.name}}</td>
            <td>{{this.value}}</td>
        </tr>
    {{/each}}
</table>